import React from 'react'; 

// Functional Component. 

const Success = (props) => { 
return ( 
	<div className="success"> 
	<p> {props.value} </p> 
	</div> 
) 
} 

export default Success; 
